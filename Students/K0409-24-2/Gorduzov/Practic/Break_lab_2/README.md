# <b>ЧЁ СДЕЛАЛ?</b>
Скрипт поломал интернет соединение. Сначала был проверен маршрут по-умолчанию. Его не было, так что добавил вручную с помощью `ip route add default via 192.168.122.1 dev enp1s0`. Потом проверил resolfe.conf, в котором был неправильный dns. Он был заменён на 8.8.8.8 и 1.1.1.1. Затем проверен файл nsswitch.conf и исправлено отсутвие dns в hosts.

# <b>ЗЗЗЗапись</b>
https://asciinema.org/a/896294