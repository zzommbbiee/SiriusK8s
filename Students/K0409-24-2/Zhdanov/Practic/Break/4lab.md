# Что сделал.
После выполнения скрипта создалось несколько сломанных бинарников. Их список выведен командами `ls -la.` У `mystery_no_exec` не было прав на выполнение их добавил командой chmod +x. У `mystery_bad_interpreter` был указан неверный интерпретатор для исполнения скрипта, его исправил в vim. `not_a_binary` оказался обычным текстом, а не бинарником. А `mystery_truncated_elf` — урезанный ELF-файл, после проверки оказалось, что восстановить не получится. 

# Запись консоли.
https://asciinema.org/a/60foakRIgKTCa8Il