Лабораторная работа посвящена бинарникам.
В выводе скрипта видно что нам нужно запустить 4 файла (попробовать)
mystery_no_exec — нет прав на выполнение (-rw-r--r--). Просто добавила права: chmod +x mystery_no_exec` и запустила. Все работает.
not_a_binary— это вообще не бинарник, а просто текст ASCII text. Его не починить.
mystery_bad_interpreter — скрипт, но шебанг ведёт в никуда (bin/no_such_interpreter_break_lab). Поменяла первую строку на #!/bin/bash и все запустилось.
mystery_truncated_elf — ELF, но обрезан. readelf ругается на выход за границы файла. Я не поняла как его восставновить (вроде никак)
![Видео как я все это чиню](../Video/breakvideo4.cast)