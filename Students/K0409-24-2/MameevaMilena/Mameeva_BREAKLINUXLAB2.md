# 2

Была проверена маршрутизация командой ip route — обнаружено отсутствие шлюза по умолчанию. 

Шлюз был добавлен вручную: sudo ip route add default via 192.168.133.2 dev ens33. 

В файле /etc/resolv.conf прописаны DNS-серверы 8.8.8.8 и 1.1.1.1. 

В файле /etc/nsswitch.conf в строке hosts проверено наличие files dns для корректной работы DNS. 

Связность проверена через ping, затем выполнен curl к avito.ru для подтверждения восстановления сети.

# Запись всех действий сохранена с помощью asciinema и загружена для просмотра.

Ссылка на запись: https://asciinema.org/a/uJ9HPlgYG4AmnQI4